#ifndef PASSWORDMANAGER_CODER_H
#define PASSWORDMANAGER_CODER_H

#include <iostream>

namespace coder {
    std::string encrypt(std::string str);

    std::string decrypt(std::string str);
}
#endif //PASSWORDMANAGER_CODER_H
